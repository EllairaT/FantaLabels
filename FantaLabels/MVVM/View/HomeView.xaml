<UserControl x:Class="FantaLabels.MVVM.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FantaLabels.MVVM.View"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="345"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="Print Label"
                  VerticalAlignment="Center"
                  HorizontalAlignment="Left"
                  Foreground="Black"
                  FontSize="28"
                  Margin="20,0,0,0" Height="37" Width="131"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="20">
            <!-- Project Name -->
            <TextBlock Text="Project Name:"/>
            <TextBox Width="Auto" MinWidth="150" Margin="0,0,0,20"/>

            <!-- Owner -->
            <TextBlock Text="Owner:"/>
            <TextBox Width="Auto" MinWidth="150" Margin="0,0,0,20"/>

            <!-- Grid for Side-by-Side DatePickers -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <!-- Entry Date -->
                    <ColumnDefinition Width="20"/>
                    <!-- Spacer -->
                    <ColumnDefinition Width="*" />
                    <!-- Expiration Date -->
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Vertical" Grid.Column="0">
                    <TextBlock Text="Entry Date:"/>
                    <DatePicker Width="150"/>
                </StackPanel>

                <StackPanel Orientation="Vertical" Grid.Column="2">
                    <TextBlock Text="Expiration Date:"/>
                    <DatePicker Width="150"/>
                </StackPanel>
            </Grid>

            <!-- Purpose -->
            <TextBlock Text="Purpose:"/>
            <TextBox Width="Auto" Height="70" TextWrapping="Wrap"/>
        </StackPanel>

    </Grid>

</UserControl>