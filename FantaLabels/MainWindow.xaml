<Window x:Class="FantaLabels.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:FantaLabels.MVVM.ViewModel"
        xmlns:local="clr-namespace:FantaLabels"
        mc:Ignorable="d"
        Title="FantaLabel"
        WindowStyle="SingleBorderWindow"
        Height="500" 
        Width="920"
        ResizeMode="NoResize"
        Background="Transparent">

    <Window.DataContext>
        <viewModel:MainViewModel/>
    </Window.DataContext>

    <Border Background="#FFC5C5C5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="367"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="75"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock Text="FantaLabel"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       Foreground="Black"
                       FontSize="22"
                       Margin="20,0,0,0" Height="29" Width="104"/>
            
            <ContentControl Grid.Row="1"
                            Grid.Column="0"
                            Margin="10,10,10,10"
                            Content="{Binding CurrentView}"/>
        </Grid>
    </Border>
    <!-- TODO: Label preview window + print option -->
</Window>
